apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trading-fronend.fullname" . }}-test-connection"
  labels:
    {{- include "trading-fronend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trading-fronend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
