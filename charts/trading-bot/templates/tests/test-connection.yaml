apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trading-bot.fullname" . }}-test-connection"
  labels:
    {{- include "trading-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trading-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
