apiVersion: v1
kind: Service
metadata:
  name: {{ include "trading-bot.fullname" . }}
  labels:
    {{- include "trading-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trading-bot.selectorLabels" . | nindent 4 }}
